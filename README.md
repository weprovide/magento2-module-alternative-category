# Alternative Category
A module which allows for an alternative category to be configured for every category.

- URL rewrites take priority, if there exists a URL rewrite this will be used rather
than the alternative category.  
